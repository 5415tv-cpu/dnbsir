# ─────────────────────────────────────────────────────────────────
# 동네비서 숏폼 미디어 워커 — 전용 의존성
# 메인 앱(dongnebiseo/requirements.txt)과 완전 분리
# ─────────────────────────────────────────────────────────────────

# 태스크 큐 (Celery + Redis)
celery>=5.3.0
redis>=5.0.0
kombu>=5.3.0

# Pydantic 설정
pydantic>=2.0.0
pydantic-settings>=2.0.0

# HTTP 클라이언트 (ComfyUI API 호출)
httpx>=0.27.0
requests>=2.31.0

# 이미지/영상 처리
pillow>=10.0.0
numpy>=1.24.0

# (GPU 서버에만 설치) AI 렌더링 의존성
# torch>=2.0.0          # GPU 서버 전용
# diffusers>=0.21.0     # GPU 서버 전용
# transformers>=4.30.0  # GPU 서버 전용
# accelerate>=0.20.0    # GPU 서버 전용

# 유틸리티
python-dotenv>=1.0.0
tenacity>=8.0.0         # 재시도 로직
